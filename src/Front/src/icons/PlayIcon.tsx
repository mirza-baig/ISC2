import { SVGIconProps } from '../types';

const PlayIcon = ({ size }: SVGIconProps) => (
  <svg width={size} height={size} viewBox="0 0 100 100" fill="none">
    <path d="M76 50L24 79L24 21L76 50Z" className="fill-current" />
  </svg>
);

export default PlayIcon;
