import ArrowRightIcon from './ArrowRightIcon';

import { SVGIconProps } from '../types';

const ArrowUpIcon = (props: SVGIconProps) => <ArrowRightIcon {...props} className="-rotate-90" />;

export default ArrowUpIcon;
